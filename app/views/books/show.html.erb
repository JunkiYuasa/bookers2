<h1>Book detail</h1>

<table>
  <tr>
    <td><%= @book.user.name %></td>
    <td><%= @book.title %></td>
    <td><%= @book.opinion %></td>
    <% if @book.user == current_user %>
      <td>Edit</td>
      <td>Destroy</td>
    <% end %>
  </tr>
  
</table>